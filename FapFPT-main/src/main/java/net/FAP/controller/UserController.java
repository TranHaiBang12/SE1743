/*
 * Click nbfs://nbhost/SystemFileSystem/Templates/Licenses/license-default.txt to change this license
 * Click nbfs://nbhost/SystemFileSystem/Templates/Classes/Class.java to edit this template
 */

package net.FAP.controller;
/*
import com.fasterxml.jackson.core.JsonProcessingException;
import com.fasterxml.jackson.databind.ObjectMapper;
import com.fasterxml.jackson.databind.SerializationFeature;
import com.fasterxml.jackson.datatype.jsr310.JavaTimeModule;
import com.mycompany.projectspringboot.json.View;
import com.mycompany.projectspringboot.model.Clss;
import com.mycompany.projectspringboot.model.StudentClass;
import com.mycompany.projectspringboot.service.AccountService;
import com.mycompany.projectspringboot.service.ClassService;
import com.mycompany.projectspringboot.service.StudentClassService;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.http.ResponseEntity;
import org.springframework.stereotype.Controller;
import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.PathVariable;

import java.io.IOException;


@Controller
*/

public class UserController {
    
/*
    ClassService csv;
    
    @GetMapping("/classes/{clss}")
    public ResponseEntity<Object> listByClss(@PathVariable Clss clss) throws JsonProcessingException, IOException {
        Iterable<StudentClass> list = scs.findByClss(clss);
        ObjectMapper mapper = new ObjectMapper();
        mapper.registerModule(new JavaTimeModule());
        mapper.disable(SerializationFeature.WRITE_DATES_AS_TIMESTAMPS);
        String result = mapper.writerWithView(View.InternalAttendance.class).writeValueAsString(list);
        return ResponseEntity.ok(mapper.readTree(result));
    }
    */
    
}
